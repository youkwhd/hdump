#! /usr/bin/factor-vm -roots=../

USING: hdump.args io io.encodings.utf8 io.files kernel
math.parser prettyprint sequences ;
IN: hdump

: main ( -- )
    get-in/out-files swap
    dup f = [ drop read-contents ] [ utf8 file-contents ] if
    [ >hex write ] each nl
    drop ;

MAIN: main
